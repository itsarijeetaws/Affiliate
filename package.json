{
  "name": "affiliate-platform-one-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix backend run prisma:generate && npm --prefix frontend run build && npm --prefix backend run build",
    "start": "npm --prefix backend run start:combined",
    "dev": "npm --prefix backend run dev:combined"
  },
  "engines": {
    "node": ">=20"
  }
}

{
  "name": "affiliate-platform-one-app",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm install && npm dedupe && npm --prefix backend run prisma:push && npm --prefix frontend run build && npm --prefix backend run build && npm --prefix backend run seed && node scripts/prepare-hostinger-output.mjs",
    "start": "npm --prefix backend run start:combined",
    "dev": "npm --prefix backend run dev:combined"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {}
}

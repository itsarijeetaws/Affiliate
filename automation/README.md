# n8n Workflows

Import the workflow JSON files into n8n and configure environment variables:

- `BACKEND_URL`
- `AUTOMATION_API_KEY`

Included workflow templates:

- Product discovery
- Daily price updates
- Article generation + WordPress publishing

You can extend these with social media nodes (X, Facebook, LinkedIn, Pinterest) after successful publishing.

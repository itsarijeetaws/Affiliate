# Shared Service Contracts

This folder documents integration boundaries and can host extracted shared packages later.

Current runtime implementations are in `backend/src/services`:

- `amazon.service.ts`
- `wordpress.service.ts`
- `content.service.ts`
- `price-update.service.ts`

# Database

Prisma schema is located at `database/prisma/schema.prisma`.

## Commands

Run from `backend`:

```bash
npm run prisma:generate
npm run prisma:migrate
npm run seed
```

> The schema defaults to MySQL for Hostinger compatibility. Switch provider to `postgresql` if needed.
